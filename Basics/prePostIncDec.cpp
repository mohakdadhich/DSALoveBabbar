#include <bits/stdc++.h>
using namespace std;
int main(){
    int a = 10, b = 5;
    // cout<<(++a)<<endl;      // phele increment fir usage
    // cout<<(b++);            // phele usage fir increment
    // cout<<(++a)*(a++);
    cout<<(a++)*(++a);
    return 0;
}